# 🧠 Stroke Risk Prediction

An end-to-end data science project predicting stroke risk — built, tested, and revised through two complete phases of investigation: a synthetic dataset whose near-perfect results turned out to be a data artifact, and a real clinical dataset that validates which findings actually hold up.

> **Looking for the original 2025 version of this project?** It's preserved in [`legacy/`](./legacy), including the original notebook, Power BI dashboards, and documentation. This README describes the current (V2) version.

---

## 📌 Project Story

This project didn't go in a straight line, and that's the point.

**Phase 1** started with a synthetic dataset and built a regression model that achieved **R² ≈ 0.999** — a suspiciously perfect score. Instead of accepting it, the project investigates *why* the score was so high: through coefficient analysis, permutation importance, and a deliberate hypothesis-testing process (engineered interaction terms, non-linear models), it's proven mathematically that the dataset was generated by a simple additive formula:

```
Stroke Risk (%) ≈ 0.5 × Age + 5 × Symptom_Count − 9
```

Every symptom carried an almost identical weight (~2.5 each) because the data was generated that way — not because every symptom is equally important medically.

**Phase 2** validates that finding against **real clinical data** (Kaggle's Healthcare Dataset Stroke Data, 5,110 real patients). This phase confronts challenges the synthetic data never had: severe class imbalance (4.87% positive cases), informative missing data, and confounded categorical variables. It shows that real risk factors — hypertension, heart disease, glucose — carry genuinely *different*, clinically plausible weights, unlike the synthetic symptoms.

**The headline conclusion:** a model's evaluation metric is only as meaningful as the data it was computed on. R² ≈ 0.999 was a true statement about how well a model fit a formula — and a misleading one about real stroke risk.

---

## 🗂️ Repository Structure

```
stroke-risk-prediction/
├── notebooks/
│   ├── 01_EDA_Synthetic_Data.ipynb        EDA + 11-model comparison + hypothesis testing on synthetic data
│   ├── 02_EDA_Real_Data.ipynb             EDA on real clinical data, class imbalance analysis
│   ├── 03_Preprocessing_Real_Data.ipynb   Missing-data handling, encoding, leakage-safe splitting
│   ├── 04_Classification_Modeling.ipynb   Imbalance-handling strategies, model comparison
│   └── 05_Final_Comparison.ipynb         Synthetic vs. real — the project's central conclusion
│
├── models/
│   ├── stroke_regression_model.joblib                  Phase 1 model (synthetic data, Linear Regression)
│   └── stroke_classification_model_real_data.joblib    Phase 2 model (real data, Logistic Regression) — used by both apps
│
├── app/
│   ├── streamlit/        Bilingual (Arabic/English) Streamlit screening app
│   └── flask/            Bilingual Flask + vanilla JS screening app (same model, same logic)
│
├── data/
│   ├── stroke_risk_dataset.csv              Synthetic dataset used in notebook 01
│   └── healthcare-dataset-stroke-data.csv   Real dataset used in notebooks 02–05
│
├── Docs/                 Project documentation
├── presentation/         Slide deck
└── legacy/               The original V1 project (2025): notebook, Power BI dashboards, docs
```

---

## 🔬 Key Findings

| Question | Synthetic Data (Phase 1) | Real Data (Phase 2) |
|---|---|---|
| Best model | Linear Regression | Logistic Regression (`class_weight='balanced'`) |
| Headline metric | R² = 0.9999950 | ROC-AUC = 0.837 |
| Dominant feature | Age | Age (confirmed) |
| Other features' weights | Nearly identical (~2.5 each) — a data artifact | Genuinely unequal: hypertension and heart disease roughly **3–4x relative risk** |
| Naive-baseline trap | Not applicable | A model predicting "no stroke" for everyone scores 95.1% accuracy with 2% recall |
| Non-linear models | Underperform Linear Regression (no non-linearity exists to find) | Random Forest collapses to **0% recall** even with class weighting; a genuine, documented limitation |

The full reasoning, code, and evidence for every row in this table is in the notebooks — this README only summarizes the conclusion.

---

## 🖥️ Running the Apps

Both apps use the **real-data model** (`stroke_classification_model_real_data.joblib`) exclusively. The synthetic-data model (`stroke_regression_model.joblib`) is kept for methodology and is not used in either app, since its near-perfect score does not reflect real stroke risk — see notebook 01 for why.

The classification threshold (53.1%, not the default 50%) is statistically derived from Youden's J statistic on the real-data test set, balancing sensitivity (80%) against specificity (77%).

### Streamlit
```bash
cd app/streamlit
pip install -r requirements.txt
streamlit run streamlit_app.py
```

### Flask
```bash
cd app/flask
pip install -r requirements.txt
python app.py
# visit http://localhost:5000
```

---

## ⚠️ Disclaimer

This project — including both apps — is an educational data science exercise, **not a medical diagnostic tool**. The real-data model achieves ROC-AUC = 0.837 and Recall = 80% on held-out test data, which is reasonable for a screening aid but should never replace professional medical evaluation. If you or someone near you shows sudden stroke symptoms (numbness, difficulty speaking, sudden one-sided weakness), contact emergency services immediately.

---

## 🛠️ Tech Stack

Python · pandas · scikit-learn · imbalanced-learn · matplotlib/seaborn · Flask · Streamlit · joblib

---

## 👤 Author

Tariq Elnaggar — Data Scientist

This is a living portfolio project: Phase 1 was built in 2025; Phase 2 (real-data validation) and both deployed apps were added in 2026 after revisiting and critically re-evaluating the original results.
